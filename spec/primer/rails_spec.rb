require 'spec_helper'

describe Primer::Rails do
  it 'has a version number' do
    expect(Primer::Rails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
